{
  "name": "@cenariox/web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npx vite --host --port ${WEB_PORT:-4173}",
    "build": "npx vite build",
    "test": "echo \"No tests for legacy web\"",
    "lint": "echo \"No lint for legacy web\""
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
